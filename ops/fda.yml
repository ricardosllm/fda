---
- hosts: "nowhere"
  connection: "local"
  gather_facts: False
  roles:
    # - { role: "setup_env",  tags: ["always"] }
    - { role: "engine", tags: ["always"] }
    - { role: "app_stack", tags: ["always"] }
