#  ---
# - set_fact:
#     deployment_env: "{{ lookup('env', 'AWS_PROFILE') }}"

# - add_host:
#     name: "localhost"
#     groups: "{{ deployment_env }}"

---
- set_fact:
    deployment_env: "{{ lookup('env', 'AWS_PROFILE') }}"

- debug: msg={{ deployment_env }}
- debug: msg={{ lookup('env', 'AWS_PROFILE') }}

- add_host:
    name: "localhost"
    groups: "{{ deployment_env }}"
